import { Box, Typography } from "@mui/material";
import React from "react";
import { Frame } from "../components/Frame";

export const Processes = () => {
  return (
    <Frame>
      <Box>
        <Typography variant="h2" noWrap component="div">Processes</Typography>
      </Box>
    </Frame>
  );
};
